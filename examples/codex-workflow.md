# Codex Example Workflow

1. Use the CLI during implementation and testing.
2. Use the API when integrating with another service.
3. Prefer `workflow full` for one-shot generation.
4. Prefer `score` and `render` separately for debug-oriented flows.
