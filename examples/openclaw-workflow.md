# OpenClaw Example Workflow

1. Ask for genre, lyrics, instrumentation, and BPM.
2. Call `workflow full` to generate the core artifacts.
3. Optionally call `references search` before generation when the user asks for stylistic references.
4. Only use advanced integrations when the deployment explicitly enables them.
