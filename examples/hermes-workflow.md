# Hermes Example Workflow

1. Gather lyrics and style.
2. Run `music-creation-engine workflow full`.
3. Return `score.pdf`, `score.mid`, and `demo.mp3`.
4. If the user wants references, run `music-creation-engine references search`.
